name: Merge Claude Code Cloud Work
on:
  push:
    branches:
      - 'claude/**'

jobs:
  merge:
    uses: 0001-labs/workflow/.github/workflows/merge-claude-code.yml@main
    secrets:
      PAT: ${{ secrets.PAT }}
